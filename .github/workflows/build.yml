name: Build and Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm install
      
    - name: Compile TypeScript
      run: npm run build
      
    - name: Copy HTML and CSS to dist
      run: |
        cp index.html dist/
        # Fix script path for GitHub Pages (remove dist/ prefix since we're in dist/)
        sed -i 's|src="dist/character-sheet.js"|src="./character-sheet.js"|g' dist/index.html
        if [ -d "css" ]; then cp -r css dist/; fi
        if [ -d "assets" ]; then cp -r assets dist/; fi
        
    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
        cname: false
